[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pnpm --filter @octofocus/api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/health"
healthcheckTimeout = 60
