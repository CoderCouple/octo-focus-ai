/**
 * @deprecated — moved to `@/lib/api/base-response`. This shim exists so
 * the file-by-file feature migration can update imports incrementally
 * without breaking the build. Will be removed once every feature folder
 * is migrated.
 */
export * from "@/lib/api/base-response";
